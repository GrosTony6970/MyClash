'use client';

export function DisplayTab({ tournamentId }: { tournamentId: string }) {
  return (
    <div className="text-sm text-slate-500">
      Display tab — under construction (tournament {tournamentId})
    </div>
  );
}
