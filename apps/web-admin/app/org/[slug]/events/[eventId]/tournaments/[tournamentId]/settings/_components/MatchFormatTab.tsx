'use client';

export function MatchFormatTab({ tournamentId }: { tournamentId: string }) {
  return (
    <div className="text-sm text-slate-500">
      Match Format tab — under construction (tournament {tournamentId})
    </div>
  );
}
