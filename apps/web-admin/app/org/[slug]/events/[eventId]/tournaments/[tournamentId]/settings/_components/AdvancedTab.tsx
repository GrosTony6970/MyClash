'use client';

export function AdvancedTab({ tournamentId }: { tournamentId: string }) {
  return (
    <div className="text-sm text-slate-500">
      Advanced tab — under construction (tournament {tournamentId})
    </div>
  );
}
