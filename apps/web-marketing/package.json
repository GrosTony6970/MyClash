{
  "name": "@myclash/web-marketing",
  "version": "0.0.0",
  "private": true,
  "description": "Static marketing/landing site at myclash.fr (apex). Stub — agent fills in during T-003b.",
  "scripts": {
    "dev": "echo 'noop until T-003b static dev server'",
    "build": "echo 'noop until T-003b static output'",
    "lint": "echo 'noop until T-003b'",
    "typecheck": "echo 'noop for static site'",
    "test": "echo 'noop until T-003b'"
  }
}
