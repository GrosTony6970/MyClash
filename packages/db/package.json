{
  "name": "@myclash/db",
  "version": "0.0.0",
  "private": true,
  "description": "MyClash Drizzle ORM schema, migrations, and DB client.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "lint": "echo 'lint: no sources yet'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "review": "node ../../scripts/check-db-review.mjs",
    "migrate:replay": "node ../../scripts/replay-db-migrations.mjs",
    "perf:fixture": "node ../../scripts/db-synthetic-fixture.mjs --check",
    "perf:explain": "node ../../scripts/db-explain-report.mjs",
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "studio": "drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.0",
    "typescript": "^6.0.3",
    "vitest": "^2.1.0"
  }
}
