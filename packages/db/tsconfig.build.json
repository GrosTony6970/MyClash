{
  // EMIT surface — see packages/rulesets/tsconfig.build.json. `extends`
  // replaces include/exclude, so both are restated in full.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "src",
    "noEmit": false
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist", "test", "src/**/*.test.ts"]
}
