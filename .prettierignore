# Build artifacts
**/.next/
**/dist/
**/build/
**/coverage/
# Emitted at the repo root by `pnpm openapi:emit` (the drift gate uses its own
# temp dir instead). Generated, so reformatting it is pointless — and without
# this entry a manual emit reddens format:check for an unrelated reason.
openapi.json

# Lockfiles + caches
pnpm-lock.yaml
**/.turbo/
# Astro writes .astro/ on every build and check. Prettier does not read
# .gitignore, so without this entry four generated files redden format:check
# the moment anyone builds the marketing site.
**/.astro/

# Local session-tool scratch dirs. Git-ignored via their own nested .gitignore,
# but Prettier does not read .gitignore — so `format:check` was failing on 53
# files nobody committed. That went unnoticed because the step never ran: it sat
# behind a failing gate in the CI job (see docs/CODE_QUALITY_REVIEW.md).
.remember/
.superpowers/

# Auto-generated by Next.js on every build. Reformatting it makes
# Prettier and Next fight over the same line forever (single vs double
# quotes) — every developer ends up with a phantom diff. Skip the file
# entirely; its content is fixed by Next and not meant to be touched.
apps/web-admin/next-env.d.ts
apps/web-public/next-env.d.ts
apps/web-staff/next-env.d.ts
